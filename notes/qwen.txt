I am working on the Zabon language learning app.

[STRICT CODE OUTPUT RULES]
DO NOT regenerate entire files. ONLY output specific blocks to add/modify using:
1. // 🔍 FIND THIS (in [filename]): \n [exact lines] \n // ✏️ REPLACE WITH: \n [new code]
2. // 📍 INSERT AFTER [function name] in [filename]: \n [new code]
3. // 📍 INSERT NEW CASES inside the switch(action) block in app.js: \n [new cases]

The alphabet-letters.md has generated the JSON data file for all Thai consonants.

The task is to output a similar message for a new chat to:
- generate JSON data for Thai Lower class consonants.
- generate JSON data for Thai Middle class consonants.
- generate JSON data for Thai High class consonants.
- generate JSON data for Thai vowels

Can kind = "letter" displays be in the alphabet language only? Default has 2 languages set.
 
